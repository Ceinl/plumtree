# {{.Name}}

Plumtree {{.Kind}} app.

## Layout

- `plumtree.json` - committed app manifest. Keep the name, type, and access in sync with the app.
- `app/main.go` - application entrypoint.
- `AGENTS.md` - notes for coding agents working in this project.
- `.plumtree/` - local development state written by `pt dev`; do not commit it.

## Run Locally

```bash
{{.RunCommand}}
```

## Deterministic Check

```bash
{{.CheckCommand}}
```

## Deploy

Deploy through a paired server record created with `pt pair`:

```bash
pt deploy
```

The clean client authenticates through the paired SSH transport and the
versioned `/api/v1` control surface; it does not use a shared deploy token or a
legacy claim route. Complete pairing before `pt deploy` opens its target.
