{
  "name": "{{.Name}}",
  "image": "{{.Name}}:latest",
  "dockerfile": "Dockerfile",
  "memoryMB": 512,
  "cpus": 1,
  "ports": [],
  "deferred": true,
  "_note": "MVP stub: the server runs the WASM artifact today and ignores this spec; the hypervisor backend is deferred."
}
